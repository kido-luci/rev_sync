name: rev_sync
description: >-
  Offline-first sync for Dart & Flutter: push-queue outbox, delta pull with
  revision cursor, conflict detection. Backend- and store-agnostic.
version: 1.0.2
repository: https://github.com/kido-luci/rev_sync
issue_tracker: https://github.com/kido-luci/rev_sync/issues
topics:
  - offline-first
  - sync
  - synchronization

environment:
  sdk: ">=3.4.0 <4.0.0"

dev_dependencies:
  test: ^1.31.0
